# Speed bench for run_nas_w39b.pbs (CALIBRATE_CHAIN=overrides/speed_bench_chain.txt):
# the production speed settings on ONE GPU in ONE job, so the arms share a node.
# name|bench||out dir|KEY=VAL;KEY=VAL   (no overrides file: the gpu preset as shipped)
# Each arm is the fixed-step bench (K=150 accepted steps per stage, 144 lanes unless
# set): its log block and the chain summary give ms per accepted step of the slowest
# lane. Submit after tools/gpu_kernel_test.pbs (it rebuilds the kernel the ffi arms
# load). Gradient arms first, in case the wall runs out; no nsys.
# Reference, job 79499 (VULCAN-JAX 023b73a, the dense tangent operator), ms per step:
# gradient fast 502 / ffi 509 at 144 lanes, ffi 434 / 869 at 132 / 264; primal fast
# 128 / ffi 104. XLA_CMDBUF=2 moved nothing there, so it has no arms here. The out
# dirs are new, so 79499's sb_* npz files stay for a G comparison on the same draws.
g_fast|bench||data/mf_g_fast|VULCAN_JAX_SOLVER=fast;XLA_CMDBUF=1;CALIBRATE_GRAD=1
g_ffi|bench||data/mf_g_ffi|VULCAN_JAX_SOLVER=ffi;XLA_CMDBUF=1;CALIBRATE_GRAD=1
g_ffi_n132|bench||data/mf_g_ffi_n132|VULCAN_JAX_SOLVER=ffi;XLA_CMDBUF=1;CALIBRATE_GRAD=1;CALIBRATE_N_DRAWS=132
g_ffi_n264|bench||data/mf_g_ffi_n264|VULCAN_JAX_SOLVER=ffi;XLA_CMDBUF=1;CALIBRATE_GRAD=1;CALIBRATE_N_DRAWS=264
p_fast|bench||data/mf_p_fast|VULCAN_JAX_SOLVER=fast;XLA_CMDBUF=1;CALIBRATE_GRAD=0
p_ffi|bench||data/mf_p_ffi|VULCAN_JAX_SOLVER=ffi;XLA_CMDBUF=1;CALIBRATE_GRAD=0
