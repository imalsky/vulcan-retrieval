# HPC-reproducibility pins (the PBS flow; validated on NAS pyt2_8_gh Python 3.10.18
# and locally on 3.12, jax/jaxlib 0.6.2, numpy 1.26.4, scipy 1.17.1).
# jax/jaxlib are PINNED here (not floored) so a standalone reproduction env matches
# the validated combination on any interpreter -- on 3.11+ a bare floor would
# resolve the latest untested jax instead (jax dropped Python 3.10 at 0.7.0).
# The package metadata (vulcan-retrieval's pyproject) declares version FLOORS for
# TestPyPI; THIS file carries the exact validated chemistry commit -- keep the
# pins at or above those floors
# (vulcan-jax>=0.17.0, vulcan-forward>=0.27.0).
# vulcan-forward is pinned the same way: it sits between the two in the DAG and
# is TestPyPI-only, so without a git pin `-e .` cannot resolve it at all.
# ExoJax is a plain pip dependency; the opacity data (k-tables, CIA) are
# seeded into the repo data/ tree, not fetched at run time.
# The PBS preflight installs the synced trees editable instead (git pull is
# the code-deploy mechanism); use this file for a standalone reproduction env.
# NOT INSTALLABLE AS WRITTEN on linux-aarch64 (the GH200): the pinned vulcan-jax
# declares exogibbs, whose declared dependency pyfastchem has no aarch64 wheel and
# would be built from source. tools/bootstrap_nas_env.pbs installs exogibbs with
# --no-deps and is the supported route on the node.
jax==0.6.2
jaxlib==0.6.2
exojax==2.2.3
numpy>=1.26
scipy>=1.12
matplotlib
corner
tqdm
vulcan-jax @ git+https://github.com/imalsky/jax-vulcan.git@d8ae8fcd0ccd5660fdb0280428cb627310d5dca4
vulcan-forward @ git+https://github.com/imalsky/vulcan-forward.git@7ee76d12afce2eb3b0bb34f3ae37bd1a437d23b2
-e .
