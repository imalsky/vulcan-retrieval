# HPC-reproducibility pins (the PBS flow; validated on NAS pyt2_8_gh Python 3.10.18
# and locally on 3.12, jax/jaxlib 0.6.2, numpy 1.26.4, scipy 1.17.1, 2026-07).
# jax/jaxlib are PINNED here (not floored) so a standalone reproduction env matches
# the validated combination on any interpreter -- on 3.11+ a bare floor would
# resolve the latest untested jax instead (jax dropped Python 3.10 at 0.7.0).
# The package metadata (vulcan-retrieval's pyproject) declares version FLOORS for
# TestPyPI; THIS file carries the exact validated chemistry commit -- bump the
# pins deliberately and keep them at or above those floors
# (vulcan-jax>=0.10.1, vulcan-forward>=0.18.0).
# vulcan-forward is pinned the same way: it sits between the two in the DAG and
# is TestPyPI-only, so without a git pin `-e .` cannot resolve it at all. Its
# commit matches jwst-transit-authority's deploy/pins.env, so a reproduction env and
# the deployed Space run the same engine.
# ExoJax is a plain pip dependency; it downloads HITRAN line lists / CIA tables
# into the repo data/ caches on first use.
# NOTE the PBS preflight installs the SYNCED trees editable instead (git pull is
# the code-deploy mechanism); use this file for a standalone reproduction env.
jax==0.6.2
jaxlib==0.6.2
exojax==2.2.3
numpy>=1.26
scipy>=1.12
matplotlib
corner
tqdm
vulcan-jax @ git+https://github.com/imalsky/jax-vulcan.git@578da7af572278d07e1dce35deae3f36b4d57af4
vulcan-forward @ git+https://github.com/imalsky/vulcan-forward.git@b72b0d251db50b92cc5f35df23c77498a95c3df9
-e .
